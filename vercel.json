{
  "buildCommand": "BASE_PATH=/ pnpm --filter @workspace/zone7 run build",
  "outputDirectory": "artifacts/zone7/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
