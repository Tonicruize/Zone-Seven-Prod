{
  "buildCommand": "BASE_PATH=/ pnpm --filter @workspace/zone7 run build && cp -r artifacts/zone7/dist/public dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
